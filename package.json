{
  "name": "dropbox-restore",
  "version": "1.0.0",
  "description": "Bulk restore deleted files from Dropbox via API",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "dropbox-restore": "src/index.js"
  },
  "scripts": {
    "run": "node src/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.1.0"
  },
  "dependencies": {
    "dotenv": "^16.4.0"
  }
}